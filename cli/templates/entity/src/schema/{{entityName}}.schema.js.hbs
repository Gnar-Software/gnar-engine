import { schema } from '@gnar-engine/core';
import { config } from '../config.js';

export const {{entityName}}Schema = {
    schemaName: '{{serviceName}}Service.{{entityName}}Schema',
    schema: {
        type: 'object',
        properties: {
            // Add your properties here
            
        },
        required: [],
        additionalProperties: false
    }
};

export const {{entityName}}UpdateSchema = {
    ...{{entityName}}Schema,
    schemaName: '{{serviceName}}Service.{{entityName}}UpdateSchema',
    schema: {
        ...{{entityName}}Schema.schema,
        required: []
    }
};

export const validate{{pascalCase entityName}} = schema.compile({{entityName}}Schema);
export const validate{{pascalCase entityName}}Update = schema.compile({{entityName}}UpdateSchema);
