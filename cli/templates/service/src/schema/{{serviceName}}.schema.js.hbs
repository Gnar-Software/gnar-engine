import { schema } from '@gnar-engine/core';
import { config } from '../config.js';

export const {{serviceName}}Schema = {
    schemaName: '{{serviceName}}Service.{{serviceName}}Schema',
    schema: {
        type: 'object',
        properties: {
            // Add your properties here
            
        },
        required: [],
        additionalProperties: false
    }
};

export const {{serviceName}}UpdateSchema = {
    ...{{serviceName}}Schema,
    schemaName: '{{serviceName}}Service.{{serviceName}}UpdateSchema',
    schema: {
        ...{{serviceName}}Schema.schema,
        required: []
    }
};

export const validate{{pascalCase serviceName}} = schema.compile({{serviceName}}Schema);
export const validate{{pascalCase serviceName}}Update = schema.compile({{serviceName}}UpdateSchema);
